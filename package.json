{
  "name": "aipassport",
  "version": "2.0.0",
  "type": "module",
  "description": "Sovereign AI Passport — decoupled, provider-agnostic context layer with hardware-enforced zero-trust boundary",
  "scripts": {
    "build": "corepack enable pnpm && pnpm -C frontend install && pnpm -C frontend build",
    "test": "pnpm test:backend && pnpm test:agent && pnpm test:frontend",
    "test:backend": "cargo test --manifest-path backend/Cargo.toml",
    "test:agent": "cargo test --manifest-path agent/Cargo.toml",
    "test:frontend": "pnpm -C frontend build"
  },
  "keywords": [
    "mcp",
    "ai-passport",
    "zero-trust",
    "tee",
    "sovereign"
  ],
  "author": "",
  "license": "Apache-2.0",
  "packageManager": "pnpm@10.28.0",
  "dependencies": {
    "@modelcontextprotocol/sdk": "^1.29.0",
    "ajv": "^8.20.0"
  }
}