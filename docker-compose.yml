version: '3.8'

services:
  aegis-passport:
    build: .
    image: aegis_passport_local
    container_name: aegis_passport_local
    restart: unless-stopped
    volumes:
      # Maps your local machine vault directory securely inside the isolated application context
      - ~/.ai-passport:/root/.ai-passport
    environment:
      - LOCAL_VAULT_PATH=/root/.ai-passport
      - R2_ACCOUNT_ID=${R2_ACCOUNT_ID}
      - R2_ACCESS_KEY_ID=${R2_ACCESS_KEY_ID}
      - R2_SECRET_ACCESS_KEY=${R2_SECRET_ACCESS_KEY}
      - R2_BUCKET_NAME=${R2_BUCKET_NAME}
    ports:
      - "8080:8080"
